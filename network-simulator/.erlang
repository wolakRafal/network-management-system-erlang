io:format("executing user profile in .erlang\n",[]).
io:format("Welcome to Network Simulator Shell. \n\n",[]).

io:format("Setting env variable JSX_FORCE_MAP=true. \n\n",[]).
os:putenv("JSX_FORCE_MAPS", "true").

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Below is legacy code for working with           %%
%% NMS from erlang shell without rebar3,           %%
%% uncomment if you want use it                    %%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%code:add_path("src/").
%%code:add_path("test/").
%%code:add_path("ebin/").
%%make:all([load]).

%%io:format("Executing Unit tests. \n\n",[]).
%% eunit:test(all_tests,[verbose]).
%%eunit:test(all_tests).

%io:format("Run eunit:test([all_tests]). to Execute All Tests  \n",[]).
