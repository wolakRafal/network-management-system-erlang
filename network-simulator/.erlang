io:format("executing user profile in .erlang\n",[]).
io:format("Welcome to Network Simulator Shell. \n\n",[]).

code:add_path("src/").
code:add_path("test/").
code:add_path("ebin/").
make:all([load]).

io:format("Executing Unit tests. \n\n",[]).
eunit:test([network,ne_device]).

io:format("Run eunit:test([network,ne_device]). to Execute All Tests  \n",[]).
